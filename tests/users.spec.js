const { expect } = require('chai');

const config = require('../src/config');

process.env.DB_NAME = 'mercury-mock';

const server = require('../index');

describe('...', () => {
	it('...', (done) => {
		// ...
		done();
	});
});
